package com.example.carrercrafter.enums;

public enum ApplicationStatus {
	APPLIED,
    UNDER_REVIEW,
    SHORTLISTED,
    INTERVIEW_SCHEDULED,
    REJECTED,
    SELECTED,
    
    RESCHEDULE_REQUESTED,
    RESCHEDULE_REJECTED
}
